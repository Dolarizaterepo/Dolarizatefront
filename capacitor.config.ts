import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.dolarizate.test.app',
  appName: 'dolarizate',
  webDir: 'dist',
  bundledWebRuntime: false
};

export default config;
